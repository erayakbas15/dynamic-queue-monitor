# Dynamic Queue Monitor

A multi-channel dynamic queue monitoring system implemented with Spring Boot and React.

The application simulates Sender and Receiver threads communicating through a shared bounded queue and provides real-time monitoring of thread and queue states.